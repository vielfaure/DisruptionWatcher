import { IArgv } from "../util/ArgvHelper";
import { Command } from "./Command";

export class JourneysRetrievalCommand extends Command {

    constructor(args: IArgv) {
        super(args);
    }

    public async execute() {
        // tslint:disable-line
    }
}
